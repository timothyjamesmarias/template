<% flash.each do |type, message| %>
  <div data-testid="flash-<%= type %>"
       class="mb-4 rounded px-4 py-3 text-sm <%= type.to_sym == :alert ? "bg-red-50 text-red-800" : "bg-emerald-50 text-emerald-800" %>">
    <%= message %>
  </div>
<% end %>