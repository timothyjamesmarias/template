default: &default
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL", "redis://localhost:6379/1") %>
  channel_prefix: template_<%= Rails.env %>

development:
  <<: *default

test:
  adapter: test

production:
  <<: *default
